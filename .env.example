# Backend API
PORT=8080
DATABASE_URL=postgresql://cicd:cicd@db:5432/cicd?schema=public

# Alerts (email)
ALERT_EMAIL_TO=
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# Worker (simulation defaults)
INTERVAL_SECONDS=8
FAILURE_RATE=0.3
PROVIDER=github
PIPELINE=sample-repo/main

# Worker (GitHub polling)
MODE=github
GITHUB_OWNER=jatinsuthartalentica
GITHUB_REPO=AI-Ticket-Estimator

# Frontend build arg (compose passes this)
VITE_API_BASE=http://localhost:8080
